<%= form_for(@login_test) do |f| %>
  <% if @login_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@login_test.errors.count, "error") %> prohibited this login_test from being saved:</h2>

      <ul>
      <% @login_test.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
